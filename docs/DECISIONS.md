# Graffik NG — Decision Log

Running, session-by-session narrative: what was decided, what broke, and what
was learned. ADRs in `docs/adr/` are the canonical *why* for load-bearing
decisions; this file is the log around them, including the process lessons an
ADR has no place for. Mirrored to the Claude project as
`claude/graffik-ng-decisions.md`.

## 2026-08-15 (kickoff, pre-Phase 0)
- Full modernization; maintained living app
- First milestone: live-action multiplicity (record/replay identical passes)
- macOS first, Windows later, Linux via CI

## 2026-08-15 (post-Phase 0 recon — see claude/phase0-codebase-reality-report.md)
- **Base repo decision REVERSED:** do NOT fork Graffik_Legacy (Qt 4.8.1, protocol incompatible with NMX — no sub-address support). Legacy is UX/feature reference only. The reboot repo (DynamicPerception/Graffik) contains the real NMX protocol implementation (~1,100 lines) — reference implementation.
- **Platform: TypeScript + Electron.** Strictly separated headless `nmx-protocol` core package; Electron shell for UI. Rationale: firmware executes moves (determinism on-device), UI is 80% of remaining work and web tech is fastest for timeline/keyframe editors, electron-builder solves macOS notarization, Windows nearly free.
- **Protocol implemented fresh from spec** (official PDFs recovered from nanoMoCo_Firmware repo + Sample Commands.txt + firmware source as ground truth), NOT ported from GPL code → new codebase's license is our choice (MIT chosen).
- Key protocol facts: packet = `00 00 00 00 00 FF` + address (default 3) + subaddr (0 gen / 1–3 motors / 4 camera / 5 key-frame engine) + command + length + big-endian payload. USB serial **19200 baud**. Queries are commands ≥100. Broadcast start/stop/pause = e-stop primitive. Firmware has native key-frame engine (what NMX Motion app uses for real-time moves).
- Reference repos: DynamicPerception/{Graffik, Graffik_Legacy, nanoMoCo_Firmware, NMXComs, NMXCommander, nmx-motion-ios}. Forks (djordan2, thisdroneeatspeople) contain nothing unmerged — dropped.

## 2026-08-15 (Phase 1 build v0.1)
- **`@graffik-ng/nmx-protocol` v0.1:** packet codec + full command vocabulary + `NmxClient` queued transport (timeout, fire-and-forget, `stopAll()` e-stop fast path). Zero runtime deps; MIT.
- **Version-drift discovery:** Sample Commands.txt and the 2015 reboot app disagree with the shipping 2018 firmware (prog mode 22 not 0x22; motor 10 = end-limit-here; send-to-start 23 not 25). 2018 firmware dispatch = ground truth; handshake queries firmware version at connect.
- Firmware behaviors encoded: broadcasts silent; jog-speed silent in joystick/Graffik mode; query floats = fixed-point ×100; broadcast enum start1/stop2/pause3/addr4/graffikUSB5/BLE6/kfStart7/kfStop8/kfPause9.
- 2-point primitives: gen 26/27 (start/stop at current pos), gen 25 (all to start), continuous mode + travel/accel/decel/easing.

## 2026-08-15 (v0.2)
- **KF engine playbook from nmx-motion-ios:** KF engine for 3+ points only (2-point = classic engine); upload order setAxis→count→videoTime→xn*→fn*→dn*→endTransmission; interior velocities solved HOST-side on cubic Hermite (max velocity until segment reverses); run = takeUpBacklash→start.
- **Implemented fresh (not GPL-ported):** spline.ts (bisection solver, ×0.995 back-off), move.ts (buildKeyFrameMove/runSequence), simulator.ts (SimulatedNmx PortLike test double). jog-slice demo mode via "simulator://nmx".

## 2026-08-16 (first launch on user's Mac — bring-up field notes)
- App runs on Apple Silicon/macOS Tahoe; tests green on user's machine.
- Potholes for onboarding docs: (1) Homebrew node llhttp dylib mismatch → brew reinstall node; (2) npm ≥11.19 allowScripts blocks install scripts → approve electron + @serialport/bindings-cpp, approvals version-pinned; (3) electron path.txt sometimes missing → one-liner fix / Electron 43 self-heals; (4) **XProtect flagged EOL electron@31.7.7 as malware + auto-trashed** (Codex-CLI-style false positive) → fixed by electron@43.4.0. Rule: never pin EOL Electron.

## 2026-08-16 (knowledge system + v0.3 autonomous slices)
- **Knowledge system in-repo** (ADRs 0000–0010, digests + HUB, CLAUDE.md) + reusable `knowledge-system` skill — installed in user's account; invoked as session bookend. Hub mirrored at claude/graffik-ng-hub.md (read first).
- **v0.3:** film.ts `.graffik` schema (ADR-0010); **timeline keyframe editor** with true-solver curve preview via pure IPC (ADR-0009) + KF upload/run + staleness guard; host-side cue countdown + pass log; firmware gate (v70 or explicit override) + jog clamp ±4000; gamepad jog (HTML5 Gamepad API); simulator progress animation.
- Editor UX anchored in Dragonframe Arc / eMotimo "jog-to-keyframe" idiom.

## 2026-08-16→17 (v0.3.1 verified + v0.4 shipped)
- **v0.3.1 verified on user's Mac** (bridge-deployed after chat-download failure — delivery lesson: use device bridge, not download dance). Fixed retina DPR canvas-layout bug (tracks were 2× tall, Pan/Tilt clipped — canvas layout math must be CSS-pixels only) and compacted layout to zero-scroll (window 900×930 useContentSize).
- **v0.4 (three green-lit tracks, all sim-verified, 53 tests):**
  1. **Camera trigger slice** — sub-addr 4 IPC (cam-arm/fire/disable) + compact UI row + simulator camera state.
  2. **Editor polish** — undo/redo (⌘Z, film snapshots), keyframe selection + inspector (numeric t/pos entry, endpoint time-lock), arrow-key nudge, ⌫ delete. **Sim physics**: tick()-based jog integration. **nmx-cli**: ports/info/run/stop, firmware-gated, SIGINT→e-stop.
  3. **Packaging + repo + CI** — electron-builder (`npm run dist` → unsigned dmg), LICENSE/CONTRIBUTING/.gitignore, GitHub Actions (test matrix ubuntu+macos, dmg artifact on main), git history initialized.
- Parity vs old Graffik/NMX Motion: jog ✔ 2-point ✔ keyframes ✔ save/load ✔ joystick ✔ **camera ✔**. Open: SMS/timelapse (deferred), multi-controller, timeline zoom.

## 2026-08-17 (repo hygiene, CI, delivery lessons)
- Repo pushed to **github.com/subvoyant/graffik-ng**. `.claude/` memory scratch is gitignored; the knowledge system (`docs/adr`, `docs/digests`, `CLAUDE.md`) is deliberately **in** the repo — it is project documentation, not agent scratch.
- **Lockfiles must be committed** (ADR-0011); CI installs with `npm ci`. Failure mode hit once: a devDependency added to `package.json` without re-running `npm install` → `npm ci` fails "Missing <pkg> from lock file". **My error, not the owner's** — recorded so the reflex is to check the lock, not to apologize.
- The **CI badge is authoritative**; do not report a run green from a page summary that cannot be verified. (Bitten again 2026-08-17: a `?query=is%3Acompleted` URL hides an in-progress run, so the top row was the *previous* commit. Check the commit title matches the SHA you pushed.)
- Commit identity: commits were authored under the Cowork address; the owner added `software@` to their GitHub account so the repo shows under their contributions.
- **Delivery lesson (repeat):** deliver via the device bridge, never the chat-download dance. The mounted FS refuses `tar` overwrite → extract to `/tmp`, then `cp`. It also refuses to overwrite one large binary (`icon.icns`, 2.3 MB) — skip unchanged binaries when copying.
- **Never run `git status` through the device bridge.** It takes `.git/index.lock` and the bridge cannot unlink it, leaving a stale lock that blocks the owner's next git command. Use `git --no-optional-locks status`, or read `.git/refs/**` directly.

## 2026-08-17 (v0.5 — UI redesign, ADR-0012)
- **Professional-3D-application idiom** adopted deliberately (Blender / C4D / Resolve conventions) so the app is legible to its actual users: token-based neutral dark palette, recessed inputs, hairline elevation, app-bar/rail/stage/status-bar frame, diamond keyframes, **drag-scrub numeric fields**, timeline zoom/pan, selected-key inspector.
- **Axis palette failed colorblind validation** — original slide/pan/tilt greens-and-oranges gave Pan↔Tilt ΔE 6.6 (deuteranopia). Replaced with validated categorical slots 1–3 (`#3987e5` / `#d95926` / `#199e70`), all-pairs PASS. **Rule: never eyeball a palette — run the validator.** New series colors come from the next validated slot, never invented.
- **Browser-preview stub** added to the renderer so `index.html` opens standalone without Electron — this later turned out to be the highest-leverage thing in the release (see v0.6).
- `docs/HARDWARE-BRINGUP.md` written *before* hardware: first-contact sequence, 5-pass repeatability method with mm thresholds, the ADR-0006 engine shootout, safe-limits worksheet.

## 2026-08-17 (v0.6 — safety, persistence, controller feel)
- **ADR-0013: soft travel limits, enforced host-side.** The NMX has no encoder feedback, so limits must be *taught by jogging*. Enforced in the **main process** at three points (jog request w/ 250 ms lookahead · 90 ms jog monitor · pre-upload film validation) so no renderer bug can bypass them. Motion *away* from a violated bound is always allowed — a rig parked outside its limits must stay recoverable. Firmware-side limits (motor cmds 9/10/33) rejected as the primary mechanism: unvalidatable without hardware, and they mutate persistent controller state other apps see. Revisit after bring-up.
- **Preferences persistence** (`userData/preferences.json`): window bounds, last port, jog speed, limits, gamepad config, recent files. `lastPort` is **remembered but never auto-connected** — opening a serial port stays an explicit human act.
- **Gamepad bindings + ballistics** (owner request): per-axis mapping with a "bind…" learn mode that captures whichever stick moves (controller axis numbering is not standardized, so never make the user know indices), invert per axis, deadzone / response-curve exponent / max-speed scaling, live response-curve plot. Ballistics **renormalizes after the deadzone** — a naive `raw^curve` leaves a speed discontinuity at the threshold, which on a slider is a visible jerk in the shot.
- **Headless-render verification became part of the loop.** Rendering `index.html` in Chromium (via the v0.5 preview stub) and *looking at the picture* caught three defects code review missed: (1) taught limits were **invisible** on the timeline whenever they sat >15% outside the move, because auto-scale ignored them — the reassurance failed exactly when it mattered; (2) the rail overflowed and scrolled as soon as a keyframe was selected; (3) the last ruler label rendered clipped. **Keep this step: build it, render it, look at it.**
- Same render produced the **fresh README screenshot** — no need to ask the owner for a grab.
- **The rail's vertical budget is now spent.** Worst case fits with 0 px to spare at the default 820 px window. The next piece of always-visible configuration goes in a **modal**, not a rail panel.
- 57 core tests green.

## 2026-08-17 (v0.7 — timecode, 3D export, event schema)
- **ADR-0014: frames are the authoring unit; milliseconds exist only at the protocol boundary.** Milliseconds had propagated outward from the firmware until they reached the operator, who does not think in them. `.graffik` **v2** stores `timebase` + `durationFrames` + `cueFrames` + `startFrame` + per-keyframe `frame`; conversion happens in `filmDurationMs`/`filmCueMs`/`filmAxesToMs` and nowhere else.
  - **Rates are exact rationals** — 23.976 = 24000/1001, never the decimal. The rounded decimal drifts ~3.6 ms per 1000 frames; between two passes of a composite that is visible misregistration.
  - **Drop-frame is legal only at 29.97/59.94** and is refused elsewhere: at 23.976 the correction is 14.4 frames per 10 min, not a whole number, which is why no DF standard exists for it. Typing a DF-skipped timecode (`00:01:00;00`) is rejected, not rounded — those timecodes do not exist and accepting one hides a typo.
  - Changing the timebase **retimes** the move (same real seconds, new frame count). Keeping frame numbers would silently change a move already matched to a performance.
  - Timecode is bridged **synchronously** into the renderer by preload (`window.tc`) so there is exactly one implementation; the app now hard-depends on the core being *built*, and preload fails loudly if it is not.
  - v1 files migrate at an **assumed 24 fps**, with the assumption written into the file's `notes` rather than hidden.
- **ADR-0015: 3D export via OpenUSD + `.chan`, not Alembic.** USD is the only common format that *states its own scale* (`metersPerUnit`, `upAxis`) — every other option makes the importer guess. Alembic's Ogawa container has no maintained pure-JS writer, so writing it means a native dependency in a dependency-free core.
  - **Scale = a measured `RigCalibration`** (steps/mm, steps/deg ×2, nodal offset, head height). The NMX has no encoder, so nothing in software can derive it. Procedure added as `docs/HARDWARE-BRINGUP.md` Phase 6. Two diagnostics: a ×100 error is the unit setting; ×2/4/8/16 is microstepping.
  - Export writes the **mechanism** (Carriage→Pan→Tilt→Camera), not a flattened matrix, and samples the *same* solver as the upload, once per frame.
  - `nodalOffsetMm` is not optional in practice — a head swings the camera on an arc, and ignoring it is why tracked CG slides against a plate on tilts.
- **ADR-0016: timeline events, and where they are scheduled.** ADR-0005 applied again: *anything that must be identical between passes cannot be timed by the host.* Tier 1 (host) ≈ ±20 ms and non-repeatable — fine for cue lights. Tier 2 (cue list uploaded to a microcontroller, one `GO`) is required for focus pulls and animatronics. The UI must show which tier a cue is on.
  - Backend survey: Arduino-class serial, **Enttec DMX USB Pro** and OSC all need **no new dependency**; MIDI is the outlier (every Node binding is native, and Web MIDI would breach ADR-0007). Device protocol is versioned plain text so it can be implemented on any board and read in a serial monitor at 2 a.m.
  - **Safety gap recorded:** an armed cue list is state `stopAll()` does not reach. `ABORT` must be wired into the e-stop before any Tier-2 output drives something that moves.

## 2026-08-17 (v0.7.1 — move files vs export, and a real bug)
- **The Move panel's New/Open/Save conflated two different jobs.** Owner caught it. Split: **move file** (`.graffik` document — New/Open/Save/Save As, `⌘N/⌘O/⌘S/⇧⌘S`, dirty tracking, current-path so Save does not re-ask) versus **Export 3D…** (`⇧⌘E`, its own dialog, never touches the `.graffik`). Document identity moved to the **status bar**, which is both where 3D apps put it and the only place with room.
- **Export targets broadened** to what a real post pipeline needs: USD, **Alembic+FBX via a generated Blender headless script**, After Effects keyframe data, Nuke `.nk` (carries `rot_order YXZ`, so unlike `.chan` it cannot be imported wrong), `.chan`, CSV. Saying "no" to Alembic was correct about the writer and wrong about the user's need — the bridge costs nothing and removes the objection.
- **After Effects has no real-world units** (its 3D space is pixels), so AE export takes an explicit pixels-per-metre mapping. That is a creative decision about world scale and is exposed rather than buried in a constant.
- **Scale check in the export dialog** — live per-axis extents in mm/degrees and scene units. This is what catches a factor-of-ten calibration before it reaches another application; reading the exported file does not.
- **The save/open defect:** `prefs.recent` was the one preferences sub-object without a type guard in `loadPrefs`, and it is read with `.filter()` by exactly the save and open handlers and nowhere else — so a bad value broke precisely those two commands. Found by auditing for what the two broken paths share exclusively (could not reproduce: no Electron in the sandbox). All sub-objects guarded; `remember()` wrapped so recent-file bookkeeping can never fail a save.
- **Failures now raise a modal dialog from main**, not just a status-bar line. "Your move did not save" is not a status-bar-grade message — and had it been a dialog, the owner would have handed over the error text instead of "seems broken".
- 134 tests. Pushed as `bb177ff`.

## 2026-08-17 (v0.8 — the cue system, ADR-0016 implemented)
- **`trigger.ts` in the core.** `TriggerBackend` seam (`describe/outputs/supports/fire/arm/start/abort/close`) with a declared `tier`, mirroring the `PortLike` seam. Three implementations: `SimulatedTriggerBackend` (records what would have fired), `SerialTriggerBackend` (GRAFFIK-TRIG v1 over any `PortLike`), and `SimulatedTriggerDevice` — the **device** side, with its own `tick()` clock — so Tier-2 timing is tested end to end without a board. Same discipline as `SimulatedNmx`.
- **`CueScheduler` takes injected time** (`advanceTo(elapsedMs)`): deterministic under test, timer-driven in the app. Two deliberate behaviours: it **fires a late cue rather than dropping it** (a missed cue light is invisible; a late one is explicable), and it exposes `worstJitterMs()` so the Tier-1 caveat is a *measured number* rather than a claim in a document.
- **Unroutable cues abort the pass before it starts.** Discovering an unbound target from a log afterwards is the expensive version of the same information.
- **The serial backend refuses an unknown protocol version** rather than guessing a command set — ADR-0004's trap, applied to a second device. It also refuses a partial `ARM` (better no cues than some), and `abort()` is fire-and-forget so the e-stop path cannot hang on the device that may be what failed.
- **Reference firmware shipped:** `firmware/graffik-trig/graffik-trig.ino`. Non-blocking pulses — a `delay()` inside a cue would make every later cue late, which is the exact failure the board exists to prevent. GPI edges reported with the device's own timestamp.
- **Safety gap CLOSED:** `stopAll()` now aborts every backend **first and independently of the NMX**. If the link to the controller is what has failed, the cue about to fire is still ours to stop.
- **UI:** cue lane above the axes (cues belong to the move, not to an axis), frame-snapped, sustained cues drawn as bars. The rail inspector became **context-sensitive** (`selection = {kind:"key"|"cue"}`) — one panel serves both, which is the 3D-app idiom and the only thing the height budget allowed. ⚡ Cues… modal holds the device and the target bindings, with the tier explanation *in the dialog*, not only in the ADR.
- **Process note:** I wrote a test named "refuses an unknown protocol version" that asserted nothing meaningful — dead code and a vacuous expectation. Caught it on review and made it real by giving the simulated device a settable version. **A test that claims coverage it does not have is worse than no test**, because it removes the pressure to write the real one.
- 159 tests. Pushed as `d89ea69`.

## 2026-08-17 (v0.9 — DMX + OSC backends, measured jitter)
- **Both new backends cost zero dependencies**, which is why they were chosen ahead of MIDI. The Enttec DMX USB Pro is an FTDI virtual COM port, so it rides the existing `serialport` package and the same `PortLike` seam; OSC is `node:dgram` with the encoder written here.
- **DMX framing gotcha, now an invariant:** `0x7E | label | lenLSB | lenMSB | data | 0xE7` — the **length field is LITTLE-endian**, the only such field in this codebase. The NMX is big-endian throughout. Two byte-order conventions living in one repo is exactly how a subtle framing bug is born, so it is called out in the source and the digest. Label 6's data starts with the DMX **start code**, so `dmxPayload` puts channel N at `payload[N]`; the widget rejects universes shorter than 24 channels, so short ones are **padded, never truncated**. Per Enttec's own API the host baud rate is a dummy value — the widget owns DMX timing.
- **DMX is state, not events.** A channel holds until changed. Two consequences that fall straight out: a `pulse` must **schedule its own release** (nothing else will), and `abort()` **blacks out** — a cue system that leaves a lamp at full after an e-stop has not stopped.
- **OSC padding is the classic bug, so it is a named tested function.** An OSC-string is null-terminated *and then* padded to a multiple of 4, **always with at least one null** — `"data"` occupies 8 bytes, and the type-tag string pads by the same rule. Numbers big-endian. JS numbers are ambiguous, so the rule is stated: integers → `i`, everything else → `f`, with `{int:…}`/`{float:…}` to force it, because some receivers silently ignore the wrong type. UDP confirms nothing, and the dialog says so.
- **Real bug caught by writing the tests: I had given `DmxTriggerBackend` two clocks** — a private counter advanced by `tick()` *alongside* injected timers. The test caught them disagreeing, but the production consequence was worse: nothing in the app ever calls `tick()`, so the rate limiter would have believed no time had passed and deferred every frame after the first. It would have "worked" while being wrong. Fixed to **one injected clock** (`nowFn` + `setTimeoutFn` from the same source), with the reasoning left in the source. **Rule: if you add timing to a class, inject it — never grow a second clock.**
- The other failure was mine in the test, not the code: I forgot the OSC **type-tag string pads by the same rule as any other OSC-string**, so `,isf` is 8 bytes, not 4.
- **Jitter is now observed, not asserted.** Every completed pass logs `N cues fired · worst lateness M ms (host-timed)` and names any cue more than 40 ms late. The Tier-1 caveat had been a claim in a document for three slices.
- Trigger transports (board, DMX, OSC) all close on quit, **aborting first** — a board holding an armed list, or a lamp left at full, is not "quit".
- 190 tests. Pushed as `9bb11d1`; CI #10 green in 1m 23s (commit title checked against the pushed SHA, per the earlier lesson).

## 2026-08-18 (v0.10 — lens axes: focus, iris, zoom · ADR-0017)
Owner's framing: *"Camera motion control without focus is less than ideal."* Correct, and sharper than it sounds — the rig repeats its path exactly and a human focus puller cannot, so the one place multiplicity breaks is the lens, and it breaks in the way composite work makes obvious.

- **`.graffik` v3 adds `lensAxes` as a collection SEPARATE from the motor axes.** Tempting to make focus "axis 4"; wrong. `FilmAxis.position` means NMX motor steps, uploaded to a three-axis key-frame engine, bounded by taught soft limits, converted by a rig calibration — none of that applies to a lens. The NMX also has exactly three motor channels and all three are spoken for. And the failure modes differ: a motion axis past its bound crashes a camera into a rail end; a lens axis past its bound stalls against a hard stop, which is *how it calibrates itself*.
- **The version was bumped even though the change is purely additive** — so a v0.9 build refuses a v3 file rather than opening it with the focus pull silently missing. A move file that quietly loses a pull is worse than one that will not open.
- **A lens position is a fraction of barrel travel, 0..1 — never a distance.** Real units come from a **lens map**: witness marks pairing a travel fraction with what the barrel reads there. Confirmed against the industry reference: the Preston FI+Z MDR "uses a lens calibration sequence to determine the mechanical limits of the zoom, focus, and iris rings," re-run whenever a motor is connected — that establishes the span — and then Lens Mapping records motor position at six discrete distances plus infinity, stored per lens in a 150-lens on-board library. Our `LensMark[]` is that list. Storing feet directly would bake one piece of glass into the move file and lie the moment anybody changes lenses.
- Falling out of that, and implemented as such: **no map still repeats exactly** and displays percent, never a fabricated distance; interpolation is linear **between adjacent marks** and **clamps outside** them (a focus scale is grossly non-linear, so a global fit is wrong everywhere and extrapolation is inventing data); iris maps often **descend**, so the inverse handles that; end-of-travel labels prefer the **engraving** (`∞`) over the map's number (`60.0m`), which claims a precision the barrel never had.
- **Same solver as everything else** (ADR-0009), clamped to 0..1. Lens keys retime with duration and timebase changes — a pull is timed to the move, not the wall clock.
- **Owner supplied the hardware direction mid-slice: Arduino-class board + stepper driver boards.** ADR-0017 §4 turns that into a spec rather than leaving it open. Lens curves are **uploaded before the pass and executed device-side**, started by the same single `GO` as the cue list — ADR-0005/0016 for the third time, and focus is *more* jitter-sensitive than a cue light. GRAFFIK-TRIG **protocol v2**: `LAXIS` / `LCAL` / `LKEY` / `LSEEK`. Design notes carried into it: the host uploads **samples, not keys** (the spline stays in the tested core — no second solver); **calibrate to the mechanical stops on connect** because steppers are open-loop and the stops are the only reference a barrel offers; **TMC2209** specifically, for StallGuard4 (finds the stop without a switch) and StealthChop (quiet enough to sit next to a microphone) — an A4988 is audible on a set and cannot detect a stall; **0.8 module** pinion, the cine gear standard; and **`ABORT` stops but HOLDS position** — never homes, never releases, because a focus ring that free-wheels on e-stop is a way to lose a lens.
- **Palette: a six-slot categorical set FAILED all-pairs CVD validation** (magenta↔aqua ΔE 1.6 deutan; yellow↔orange ΔE 10.6 *normal vision*). Per ADR-0012 the answer to "I need more series colours" is **faceting, not a wider palette**: focus/iris/zoom reuse validated slots 1–3 inside their own labelled band. Identity comes from the band and the direct label, never colour alone.
- **Lens lanes draw on a FIXED 0..1 scale** (motion tracks auto-scale), so the shape of a pull is comparable between takes — and because that scale is fixed and load-bearing, both ends are labelled.
- **3D export carries the lens and omits it honestly.** USD gets animated `focusDistance` (converted by `metersPerUnit`), `focalLength`, `fStop`. An **unmapped** axis is not exported at all — a travel fraction is not a focal length, and writing `0.62` into `focalLength` produces a file that looks valid and is nonsense — so the document carries an explicit `NOT EXPORTED: …` note instead. CSV, the format people open to check things, carries **both** columns.
- **The headless render earned its keep again** (fourth slice running). Code review passed; the picture did not. (1) The live value readout was pinned to each track's **top-right corner**, where it reads as a *scale label* rather than a scrub readout and collides with any key on the last frame — moved onto the playhead, on the line it describes, flipping side near the edge. (2) The fixed 0..1 scale had **no end labels at all**, so a focus lane could be read upside down — the one lane where that matters most. Both fixed before shipping.
- **Disclosure over implication:** nothing drives a lens motor yet, so the timeline band reads `(authoring + export only — no lens device yet)` and Upload appends `focus · iris NOT sent: no lens device`. An operator who assumed otherwise finds out in the rushes.
- **Stage height budget is now spent** as well as the rail's: six tracks fit 1440×880, verified by render. A seventh lane needs scrolling tracks — a real design change, not a tweak.
- 214 tests.

## 2026-08-18 (v0.11 — the lens actually moves · ADR-0018)

Owner supplied the hardware direction at the end of the last slice — Arduino-class board plus stepper driver boards — so ADR-0017 §4 stopped being a specification and became a build.

- **One board, one `GO`.** Lens axes ride the trigger board on **protocol v2**, not a second device. A second board would mean a second port to forget to plug in and, worse, a second clock — cues and focus started by two different `GO`s drift apart, which is the whole thing Tier 2 exists to prevent.
- **A v1 cue board still connects.** ADR-0004 says never guess at a command set you do not know; v1 is one we *do* know. Refusing a working cue board because the app grew a feature it lacks would be the rule applied past the point where it protects anyone. `SUPPORTED_TRIGGER_PROTOCOLS = [1, 2]`; anything else is still refused outright, and the UI says "no lens axes — cues only" rather than "not connected", so nobody goes looking for a cable.
- **The host sends samples, not keys — and decimates them under a stated bound.** Putting the spline in an `.ino` would create a second motion solver and the curve the operator draws would stop being the curve the rig pulls. But per-frame is 1731 points for a three-lane 24-second move: 34 kB down a serial line while a performer stands still. Douglas–Peucker with a **vertical** error metric fixes it — vertical, not perpendicular, because perpendicular distance mixes milliseconds and travel units into one number whose answer changes if you express the move in seconds. Vertical distance bounds exactly the thing worth bounding: *how far the device's linear interpolation can be from the spline, at any instant*, in the same units the wire uses — so the bound can be stated in **motor steps**. At half a step on a 4000-step barrel: 132 points, 2.4 kB, 0.21 s. Nothing observable given up, because the residual is finer than the motor can resolve. Consequence recorded in the firmware: **it may only interpolate linearly.** "Improving" it with smoothing would silently invalidate the number.
- **The peak rate for the feasibility pre-flight is measured on the DENSE curve.** A decimated chord's slope is a weighted average of the slopes it replaces, so it can never exceed the dense maximum — measuring dense is the conservative choice and means decimation can never quietly hide a snap focus from the check that would have warned about it.
- **Uploads are chunked and cross-checked.** An ATmega's UART buffer is 64 bytes and in principle the firmware keeps up — but "in principle the firmware is never busy" is exactly the assumption that produces a silently truncated focus pull. `LSYNC` every 32 points, plus `ARM` reporting its own lens count for the host to compare against what it sent. Over-capacity behaves identically by construction: the board drops what it has no room for, the count comes back short, the pass is refused. There is no path where a truncated pull runs.
- **Calibration is mandatory and only the board may claim it.** A stepper is open loop; `LCAL` against both stops is the only thing that makes a position mean anything. The host remembers the travel figure between sessions **as a pre-flight hint only** — because only the board knows whether it has seen a stop since it powered up. An axis with a curve and no homing makes `GO` answer `LERR` instead of `STARTED`, and the app turns that into a sentence rather than a timeout the operator has to guess at.
- **`ABORT` stops and HOLDS.** Never homes, never releases, drivers stay enabled. A focus ring free-wheeling under a heavy lens on a tilted head is how you lose a lens. The host also clears its uploaded-point count on abort, so the next `ARM` cannot "pass" by comparing against a run that was abandoned.
- **I got `invert` wrong in v0.10, and fixed it rather than documenting around it.** ADR-0017 put "motor runs backwards" on the lens axis, in the move file. Which way a motor turns is a fact about a rig on a day; a file carrying it reverses somebody else's focus pull the moment they open it — precisely the failure ADR-0016 moved cue bindings into preferences to avoid. **Schema v4** strips it and writes the fact into `notes` (the v1→v2 timebase idiom), the setting moves to prefs, and the firmware reconciles handedness at the DIR pin where it physically lives. Display, 3D export and the wire encoder all got *simpler* — which is usually how you know. Two schema versions in two days is the price of having got it wrong; a file that behaves differently on different rigs would have been the price of leaving it.
- **The reference firmware was the only part of the repo nothing checked. Now it has two checks, both in CI, and they found two real bugs on first run.**
  1. **A host exercise** — `Arduino.h` shimmed on the desktop with a fake clock, a pin array, and a *simulated barrel with hard stops* that moves on STEP rising edges in whatever direction DIR says (so it exercises the handedness handling rather than assuming it). Caught `seekStop` backing off a stop in the **wrong direction**: standing on the low stop and about to travel up, it stepped *down*, went nowhere, and returned zero travel. Every calibration would have looked like a dead motor.
  2. **A protocol parity check** — one script file fed to both the TypeScript `SimulatedTriggerDevice` and the C++ firmware, replies diffed with clocks normalised. This is the one that matters: the host is written against the simulator, so **a simulator more permissive than the board means every test passes and the rig fails.** Caught the simulator accepting `LAXIS 9` on a three-axis board. The script file is now the contract — add a case whenever the protocol grows.
- **One pre-flight gate for the whole pass.** Unroutable cues and infeasible lens speeds are reported together, because two gates are two chances to skip one and both failures cost the same thing.
- **The render pass earned its keep again** (fifth slice running). The ⌾ Lens… modal had the jog slider down in the motor block, which broke the marking loop — the workflow is *drive the barrel to a witness mark, read what it says, type it in*, so the jog belongs beside the "At" field it fills. It also wrapped the Calibrate button onto its own line where it read as unrelated to the device chip. Both regrouped; the marks list is now capped and scrolls, because a real lens map runs to a dozen marks and the sheet must not outgrow the window.
- 248 core tests · 19 firmware checks · protocol parity across 21 commands.
- **Process failure, mine.** I handed over a command block containing `npm test --prefix packages/nmx-protocol   # 248`. **zsh does not enable interactive comments by default**, so `#` and `248` went to vitest as test-name filters and it exited with "No test files found" — the suite never ran on the owner's machine, and v0.11 was committed and pushed without it. CI covers it, but the instruction was wrong and it read like a broken repo. Fixed at the root: **`./scripts/check.sh`** now runs build, typecheck, core tests, CLI smoke, firmware exercise and protocol parity in CI's own order, so the handed-over command is one line with nothing to misparse. **Rule: never put a `#` comment in a command line handed to someone else's shell.**

## 2026-08-19 (v0.12 — the lens library · ADR-0019)

ADR-0017 put lens marks in the move file. Right for one move, wrong for a shoot: a 35 mm prime has the same marks on Tuesday it had on Monday, and marking a barrel is careful physical work — drive to a distance, check it against a tape, write it down, repeat. Ask for that every setup and it stops happening, and the lane quietly degrades to percent, which repeats perfectly and tells the operator nothing. Preston stores 150 lenses for exactly this reason; the number is what a rental house's worth of glass looks like.

- **Merge on `id`, never on name.** Two people can call a lens "35mm" and mean different glass, and the same lens renamed is still the same lens. Names are for humans, ids are for merging. Ids are readable and derived (`focus-zeiss-cp-3-35mm-k3f9`) rather than opaque UUIDs so a library file stays human-editable — and the entropy is the caller's, keeping the core pure and testable.
- **Import merges and reports; it never replaces.** Someone hands you their library and losing your own marks in exchange is not an import, it is an accident.
- **One malformed entry does not sink the file.** 150 lenses with one bad one: refusing the 149 good ones helps nobody. Survivors go in, casualties are named. Deliberately the **opposite** of how a move file is treated — a move is one indivisible thing whose partial load would be a wrong move; a library is a collection whose partial load is a smaller collection. Worth stating out loud because "be strict about input" is otherwise a reflex.
- **Forgetting a lens does not unmark a move that is using it.** A delete that silently strips marks from an already-marked move is the worst kind of destructive: invisible until the pull comes out wrong. The lane keeps its marks; only the shortcut is gone, and the status line says so.
- **Applying a lens takes an undo snapshot.** It edits the move, so ⌘Z has to reach it.
- **Entry validation delegates to the map validator** rather than growing a second, subtly different set of rules. An entry that would be rejected as a map cannot be used, and finding that out at apply time is finding out too late.
- **The button reads Keep or Update depending on whether the library already has this lens.** "I re-marked this lens" and "I have a second lens with a similar name" are different intentions and the UI should not make you guess which one it heard.
- Render pass caught one thing: the library delete button was a bare `✕` sitting beside a dropdown, where it reads as "clear the selection" rather than "delete this lens permanently". Now labelled **Forget**, which also matches Keep/Update as a verb.
- 264 core tests · 19 firmware checks · protocol parity across 21 commands, all via `./scripts/check.sh`.

## 2026-08-19 (v0.13 — commissioning · ADR-0020)

No steer from the owner this round, so I picked the thing ADR-0015 had been waiting on since v0.7: *"steps→mm/deg needs a calibration MEASURED on the rig; without it an export is a shape, not a camera move."* Five versions of saying that, a procedure in a doc, three number fields in the export dialog, and nothing in between that helped anybody.

- **The measurement is physical and cannot be automated. Everything around it can be.** Reading the step counts, the arithmetic, the error estimate, noticing the answer is a power of two out, remembering the result. Leaving all of that on a clipboard is how a rig ends up with a calibration nobody can defend.
- **Spans, not absolute positions.** An observation is `{steps moved, what the tape said}`, and each contributes its own ratio. A least-squares line through absolute positions is more statistically conventional and has an **intercept** — and the intercept quietly absorbs backlash and measurement offset. A fit that absorbs your errors has stopped telling you about them. The spread between independent spans IS the error bar.
- **Spread is peak-to-peak, not deviation-from-the-mean.** Caught by a failing test I had written with the wrong expectation, and it turned out the metric was the thing that was wrong: with two measurements, deviation-from-the-mean is exactly half the disagreement the operator can see between their own two numbers. A warning that says "these disagree by 5%" while printing 2.5% teaches people to distrust it.
- **With two disagreeing measurements the app refuses to name a culprit.** Both sit the same distance from their own mean; nothing in the arithmetic can say which is wrong, and naming one would be believed. `worst` is null below three spans and the warning asks for a third. This came out of the same failing test — my expectation named the "suspect" observation, and there was no basis for it.
- **Diagnostics compare against the STORED value, not a textbook one.** "You measured 320 and the file says 160" is actionable; "that is unusual for a slider" is not. ADR-0015's two traps get named explicitly: ~×100 is a unit slip, a clean power of two is the driver's microstep jumper. Both produce a number that looks entirely reasonable alone and is an order of magnitude out in the export.
- **The laser method for rotation**, because a protractor taped under a tripod is not a measuring instrument. Laser square at a wall, mark, rotate, mark, measure between: `atan(offset/distance)`. Warns above 25° where "started square" stops being free and below 5° where the angle is too small to read.
- **Repeatability reports bias and scatter separately.** A consistent offset every pass is backlash or belt take-up and is largely correctable; scatter either side of zero is lost steps and is not. One number would hide the distinction exactly when it matters.
- **The panel carries its own jog buttons and its own live position readout.** The procedure is jog-to-a-mark, walk away with a tape, come back and type — a dialog covering the rail's jog controls would make the thing it exists for impossible. The render pass caught the second half of that: I had jog buttons but no step readout, so you would have been jogging blind inside the panel that exists to read step counts.
- **The render also caught dead code I had written myself.** `degreesFromLaser` and `laserAngleWarning` were in the core with tests and no way to reach them from the app — the same failure as a test that asserts nothing, one level up: capability the project claims and does not offer. Wired into the panel for pan and tilt.
- **A real latent bug, found by asking what happens next.** `-apply` writes main's calibration, but the export dialog keeps its own `exPrefs` copy and writes it back wholesale on any field change. Without merging the applied values into that copy, the next keystroke in the export dialog would silently overwrite the numbers just measured — invisible until an export came out at the old scale.
- 290 core tests · 19 firmware checks · protocol parity, all via `./scripts/check.sh`.

## 2026-08-19 (v0.14 — physical controls · ADR-0021)

Picked the backlog item that had been sitting since v0.6 labelled "gamepad button bindings (e-stop on a button)". That label undersold it: **until this slice the only e-stop was a mouse target**, and when a camera is heading somewhere it should not be, the operator is looking at the rig with a controller in their hands, not at a cursor.

- **The rule, and it is deliberately asymmetric: stopping is instant and always available; starting requires deliberation.** STOP fires the moment the button goes down — no hold, no confirmation, no check on what the app is doing. An e-stop that asks a question is not an e-stop. Anything that can put the rig in motion is a **600 ms hold**.
- **Hold, not double-press.** A bouncing or sticky button can produce a double-press on its own; nothing produces 600 ms of continuous contact. A hold can also show progress while it happens, so the control teaches itself instead of surprising people.
- **The latch fires once per press, not repeatedly while held.** A held button is one instruction; a Run pass that retriggered every tick would restart the move the instant it finished.
- **Two loops, and the split is the point.** The jog loop only runs when gamepad jog is toggled on. Hanging the e-stop off that would have made it work *only while jogging* — and the moment you most want a physical stop is during a **programmed pass**, which is exactly when jog is off. The button loop runs at 40 ms whenever a controller is present, whatever dialog is open, whatever has focus.
- **Nothing is bound by default.** A guessed e-stop button is worse than none, because the operator would believe in it. Button indices are not standardised across devices, so any default is a guess. The absence is shown in red instead, and two actions on one button is reported as the ambiguity it is.
- **The policy is bridged from the core, not copied** — same reasoning as the timecode bridge. "Starting requires deliberation" having two homes is how it quietly stops being true in one of them. A test walks the action list and fails if the stop action ever grows a hold or a motion action ever loses one.
- **A live bug, found by reading the loop I was about to extend.** The jog poll did `if (!pad || !connected) return;` — so a controller whose USB dropped mid-jog left **the last commanded jog speed still running**. The firmware's joystick watchdog would have caught it eventually, which is what it is for, but "eventually, by a timeout we have never measured" is not how a moving camera gets stopped, and nothing told the operator. Now it zeroes every axis immediately, says so, and logs it.
- **The render pass earned it again, twice.** Adding a Buttons section pushed the gamepad dialog past its sheet and **silently clipped the live-response canvas** — fixed generally (no `.sheet` may clip its own content) rather than locally. And the hold-progress bar was present, correctly styled, correctly coloured, and **zero pixels tall**: a flex child shrinking to nothing in an over-full column. That one was only found by *measuring* the rendered geometry rather than looking at the picture — worth remembering that looking is not always enough.
- 307 core tests · 19 firmware checks · protocol parity.

## 2026-08-19 (v0.15 — the connection doctor · ADR-0022)

Owner said hardware lands in two days. That changes what is worth building: the likeliest way a first bring-up session gets eaten is not a bug in the editor, it is ninety minutes on a handshake that times out.

- **One symptom, six causes.** Charge-only cable, unpowered NMX, BLE mode instead of USB, wrong port picked from a list that is mostly Bluetooth, non-default device address, wrong baud rate. All of them present as the same timeout, so they get told apart by swapping things one at a time. Every piece of evidence needed to separate them was already observable — the app just never looked.
- **Count the bytes. Silence and noise are different failures.** Zero bytes means nothing is talking to us. Bytes that never form a valid frame means something *is* talking and we cannot hear it — which is a baud mismatch, or a different device on that path. A timeout throws that distinction away; the probe taps the port's raw data alongside the client so it survives. This is the single most useful thing in the slice.
- **Ask every plausible address, and then say you asked.** A non-default address is a legitimate configuration — it is how two controllers share a bus — and it presents as a completely dead link. The probe asks 3, 2, 4, 5, 6, 7, 8 and stops at the first answer. When silence comes back, the report states that addresses have been ruled out, so nobody spends more of the afternoon on one.
- **The classification is a pure function.** `explainProbe(evidence)` → verdict, headline, ordered steps. No port, no timers, no IO, so the *reasoning* is what is under test rather than the plumbing, and it can be reviewed by reading it.
- **Rank the port list; never hide anything.** Bluetooth and debug entries are marked, not removed. Hiding a port the operator can see in the system would look like a missing device — a worse problem than a cluttered list.
- **The Diagnose button appears at the moment of failure** and disappears on success. Offered before there is a problem it is clutter nobody reads when it matters.
- **Failing to OPEN the port is its own diagnosis** and a much more specific one than any probe could infer: something else has the port — a serial monitor, the Arduino IDE, a second copy of the app.
- **`SimulatedNmx` gained `off()`.** The probe taps a port temporarily and must detach; a real `SerialPort` can, and a simulator that cannot is a simulator that quietly leaks. Same rule the firmware parity check enforces: **the simulator must be at least as capable and at least as strict as the thing it stands in for.** There is now a test that fails if the probe leaves a listener behind.
- Deliberately NOT done: auto-connecting to whatever address answers. Convenient, and it hides a rig configuration the operator should know about — two controllers silently adopting addresses is a much worse afternoon than one clear message.
- 324 core tests · 19 firmware checks · protocol parity.

## 2026-08-19 (v0.16 — creep, and a report that leaves the room · ADR-0023)

Two last things aimed at the session in two days. Both only matter on a first bring-up, which is exactly why they are easy to leave undone and expensive to leave undone.

- **An axis nobody has taught jogs at 500 steps/s.** ADR-0013 made soft limits un-bypassable and cannot help with the *first* jog, because **limits are taught by jogging to them** — so the procedure necessarily runs limit-less at the moment the operator knows least about the machine. A slow collision is a recoverable noise; a fast one bends a rail or drops a camera off a head. Every motion-control operator creeps on first motion; the software should default to what a careful person would do anyway.
- **It self-clears, and there is deliberately no override switch.** Teach either bound and full speed returns — one button press. An override is a thing you leave on by accident, and here the escape hatch is the action you were about to take regardless. Zero passes through uncapped, so nothing about stopping is ever slowed.
- **The 500 is a judgement, not a measurement.** If it is uselessly slow for teaching limits on a real slider it is simply wrong, and it should change against what the rig does rather than be defended.
- **A bring-up report.** A hardware session produces facts that exist nowhere else — firmware version, which port answered, travel taught, spans measured, whether five passes returned to the same place, what went wrong and what was done about it — and by default they live in somebody's memory until the next morning. One button, one markdown file.
- Three rules make it worth reading: **everything unmeasured says "not measured" rather than being omitted** (a report that drops what nobody got to reads like a complete one, and the point is knowing what is still unknown); **warnings are spelled out, not counted** ("1 warning" tells nobody anything); and **measured-but-not-applied is flagged explicitly** — measuring a calibration and never pressing *Use these numbers* is the easiest way to leave a session believing a number is in effect when it is not, and a table with two numbers in it puts the noticing onto the reader.
- The pass log comes from the **renderer**, verbatim and newest-first: main has never seen it, and on a first bring-up nobody yet knows which line mattered. `bringUpReport` is pure — caller supplies the timestamp — so a session always renders the same bytes.
- Writing the report caught a reporting gap in itself: a lens motor that is **configured but never calibrated** was being collapsed into "no barrel calibrated". Those are different facts and only one of them tells you what to do next.
- 340 core tests · 19 firmware checks · protocol parity.

## 2026-08-19 (v0.17 — a dead-export audit · ADR-0024)

Seven versions in one session, none of it hardware-verified. Before adding more surface, the question worth asking is whether what already exists holds together — so instead of a feature, a check.

- **The bug it targets happened here and was caught by luck.** `degreesFromLaser` shipped in v0.13 with passing tests and no way to reach it from the app; I noticed during a render review, by accident. **A function that is exported, tested, and callable by nothing is worse than one with no tests** — it claims coverage, so it removes the pressure to look.
- **Reachability as a fixpoint over symbol BODIES, not files.** Seed from what the app/preload/CLI name, then expand through the bodies of reachable symbols until nothing changes. Per-symbol matters: `degreesFromLaser` sits in `commission.ts` beside `fitCalibration`, which the app *does* use, so a file-level rule would have missed the exact bug it was written for.
- **Two iterations before it was worth keeping.** The first version flagged 95 of 208 exports — noise on that scale gets a check ignored, or gets real code "fixed" to silence it. Two corrections made it signal: exclude **type-only exports** (the app is JS and never names an interface — "unreferenced" is correct for those), and put **private helpers in the graph but never report them** (`SubAddress` looked dead because it is used only by `commands.ts`'s private packet builders).
- **11 live findings in 142 exports, and one of them was a pattern rather than an oversight.** The renderer had reimplemented **four** lens helpers — the mark-interpolating formatter, the axis constructor, and both library conversions — because nothing bridged the originals. That is precisely the duplication ADR-0014 built the timecode bridge to prevent, reached by the identical route: a tested pure function in the core with no path to the UI, and a UI that needs the behaviour anyway. Now `window.lens`; the copies are gone.
- **It found a stale digest, which I did not expect.** `eventsInWindow` was deleted as dead — and the nmx-protocol digest had described it as the Tier-1 host dispatch mechanism for **nine versions** after `CueScheduler` replaced it. "A stale digest is a bug" is a rule in CLAUDE.md; this is the first thing that ever caught one. **The audit is a doc-freshness check by side effect.**
- `lensPositionFor` was **kept and bridged** rather than deleted — the inverse mark lookup is what "drive the lens to a marked distance" needs, and its descending-scale handling is subtle enough to be worth keeping tested. The audit is a prompt to look, not a verdict.
- **I broke two test files with a greedy regex while cleaning up** and had to restore them from the v0.16 tarball. Deleting test blocks by pattern removed a whole `describe` body and left an empty suite. Redone by hand. **Rule: never bulk-edit tests with a regex — the thing that tells you it went wrong is the thing you are editing.**
- Verified by canary: added a deliberately unreachable export, confirmed it is reported as *unreferenced*; added a test for it, confirmed it moves to *tested but unreachable* and that `--strict` exits non-zero. Then reverted.
- 337 core tests · 19 firmware checks · protocol parity · no dead exports.

## 2026-08-19 (v0.18 — the playhead follows the pass · ADR-0025)

Owner, watching a pass run: *"The playhead should move across the lanes when a run is initiated. The blue line on bottom right moves but we should also see position in the lanes."* Correct, and slightly embarrassing — both engines were already polling the controller for progress and putting it in a bar, while the timeline, the thing an operator actually looks at, sat still. **The most useful readout during a pass was the one not being drawn.**

- **The firmware's percent is the truth; the local clock only smooths.** The device runs the move off its own crystal (ADR-0005), so a host timer started at GO drifts away from it and the playhead would lie about where the rig is — quietly, and worse the longer the move. Anchored to the controller's reading on every 500 ms poll, interpolated at 40 ms between them, and **extrapolation capped at one poll interval** so a stalled controller shows a stalled playhead rather than a confident one that has run ahead. The direction of authority is easy to get backwards: the host may smooth what the device says, never assert it.
- **A classic pass is not the timeline.** The lanes show the key-frame move; a 2-point pass runs between taught marks, is a different move, and has its own duration. A normal playhead sweeping those curves would be the statement "the rig is here, on this curve" — false. It sweeps anyway (elapsed time is true whichever engine is running) but in warning amber with the tag `2-POINT PASS · these lanes are not running`. Marking it beat hiding it: hiding would have thrown away the true part to avoid the false part.
- **The render caught a colour collision I should have predicted.** First attempt drew the running KF playhead in `--accent` — which is *the same blue as the Slide trace*, so the playhead vanished into its own curve. ADR-0012 already had the answer: **chrome wears ink, the palette belongs to the series.** Now full-opacity white at 2px versus 55% at 1px idle. Amber stays the exception because it is carrying a warning, not a state.
- **Every stop path freezes the sweep** — STOP ALL, the physical e-stop button, KF stop. A playhead still moving after a stop is the UI asserting motion that has been halted, which is the one thing a stop must never leave ambiguous. On normal completion it stays at the end of the move, because that is where the rig is.
- The view pans to follow when zoomed in, or the sweep is invisible on exactly the long moves worth watching.
- Side effect worth noting: the classic-vs-keyframe distinction is now **visible in the timeline**. It had only ever been stated in a status-bar chip.

## 2026-08-19 (v0.19 — making the playhead actually smooth)

Owner: *"The movement of the playhead is very jerky, can it be smooth?"* Three independent causes, and none of them was the drawing — which is the part I would have suspected first.

- **`setInterval` at 40 ms.** Not display-synced, so ticks landed early or late against the compositor. `requestAnimationFrame` instead.
- **`Math.round` on the frame number.** The playhead could only sit on whole frames, so on a zoomed-in timeline it *hopped* between them rather than moving. The drawn position is a float now; only the readout rounds, and `endPassSweep` puts it back on the grid because the rest of the editor assumes it lives there.
- **The re-anchor was a snap** — and this was the loudest one, and it came from the design rather than an oversight. Every 500 ms poll assigned the controller's percent straight to the drawn position, so any disagreement with the extrapolation became a visible jump twice a second. I had written "the local clock only smooths between readings" in the ADR and then implemented the readings as assignments.
- **The fix: converge by adjusting SPEED, never by assigning position.** The reading moves a *target*; the drawn position approaches it at a bounded, non-negative rate, so `shownPct` is monotonic by construction and cannot step backwards. Ahead of the prediction, speed falls to zero and it waits; behind, speed rises, capped at 3×. **A brief pause reads as steady; a reversal reads as broken.**
- **My first attempt at that fix was itself wrong, and measurement caught it.** I clamped with `Math.min(predicted, …)` — which yanks the playhead *backwards* the instant a reading arrives behind the extrapolation. The harness reported 2 backwards steps. **Reading the code had not caught it; running it did.** That is the second time this session that measuring the render beat looking at it (the first was a 0px-tall progress bar).
- **`render()` costs 0.74 ms** on a six-lane 30 s move, so 60 fps was never in question — worth measuring, because "redraw less often" would have been the obvious and completely wrong fix.
- Also removed: `syncInputs()` was being called on every tick, rewriting every field in the rail sixty times a second to update two labels.
- **The harness is NOT shipped**, and that is a deliberate call I nearly got wrong. It needs Playwright, which this repo does not depend on (ADR-0008 dependency hygiene), so committing it would have meant shipping a script that errors on the owner's machine — the exact dead-code problem the v0.17 audit exists to catch, committed the day after building the audit. The *method* is written into the digest instead: sample the drawn position every frame while feeding deliberately disagreeing readings, and check backwards steps and velocity spikes. Honest position: render-and-measure verification currently happens in my environment, not theirs.
- Measured after: 60 fps, 0 backwards steps, 0 velocity spikes over 3× median.
- **And then a worse one, from pulling that same thread.** The jog-slice digest's *Known gaps* paragraph was frozen at roughly v0.9 and still asserted three things that had shipped: "no lens motor driver" (v0.11, ADR-0018), "no per-lens map library" (v0.12, ADR-0019), and "gamepad button bindings — only axes are mappable" (v0.14, ADR-0021). The hub's digest index still stamped both major digests @ v0.9. **A digest that under-claims is more dangerous than one that over-claims** — the next session reads "no lens motor driver" and builds one. This is the third instance of the same failure this session (`eventsInWindow`, the 40 ms tick, now this), and the pattern is specific: *additive* prose gets updated because you are writing about what you just added; **negative** prose — gap lists, limitation notes, "not yet" claims — never gets revisited, because nothing about shipping a feature makes you re-read the sentence that says it does not exist. Worth a check that greps gap/limitation sections against the ADR index; not built yet, and noting that rather than pretending the fix was systemic.
- **Caught at packaging: an orphan screenshot and a stale digest line.** `docs/images/pass-playhead-classic.png` was sitting untracked and referenced by nothing, and the digest's playhead section still opened with "the 40 ms local tick" three bullets above the amendment that replaced it. Same failure mode as a dead export, one layer up — an artifact nothing points at, and prose that contradicts the code beneath it. Both fixed before the tarball went out: the shot now illustrates the classic-pass bullet it belongs to, and the opening bullet says `requestAnimationFrame`.

## 2026-08-19 (v0.20 — the runner was lying, and packaging is what caught it)

Chasing the stale gap list into `nmx-cli` turned up something worse than stale prose. `nmx run` accepts a `.graffik`, and a `.graffik` can carry three subsystems executed by three different things — NMX motion, cues, lens axes. The CLI opens a link to the NMX **and nothing else**. It was loading the cues and the lens program, ignoring both, running the motion axes, printing `pass complete`, and **exiting 0**.

- **The exit code is the part that matters.** A human might notice a missing focus pull. A script that gets 0 back has been told the pass happened, and there is no second chance — the take is shot. `nmx run` now exits 1, names each subsystem it cannot execute, and runs only with `--motion-only`, which is the operator saying it back. Separate flag from `--force`: one flag with two meanings is weaker at both.
- **The app deliberately does NOT refuse**, and that asymmetry is the actual decision. It logs `N lens lane(s) NOT driven — pull by hand` at arm time and runs, because someone is looking at the screen at that moment and hand-pulling focus over a repeating rig is a legitimate workflow — it is how the shot would have been done before lens motors existed. The rule is **whether a human is in the loop at the moment of the decision**, not GUI versus terminal. An unattended batch mode in the app would take the runner's rule.
- **Written down as the weak point rather than smoothed over:** the app's warning is a pass-log line, not a gate. If a take is ever lost to an undriven lens lane, promote it to a confirm-on-arm and supersede ADR-0026.
- **The CLI enforces no soft limits at all** — ADR-0013 lives in the Electron main process against limits in *its* preferences, so the CLI is not a quieter app, it is an unguarded one. That includes the pre-pass `sendToPosition` that walks every axis to the first keyframe, which is a real unbounded move nobody was thinking of as one. It now says so on every run. **That is a disclosure, not a fix**, and the ADR says so in those words so it does not get remembered as one.
- **Nothing tested this.** The CLI's entire CI coverage was `nmx info --sim`. `check.sh` now asserts the refusal *and* runs a full `--motion-only` pass against the simulator — six seconds, and the first end-to-end exercise of the run path that has ever existed. A fixture at `packages/nmx-cli/test/lens-move.graffik` exists to be refused.
- Two days before hardware, this is the one that would have cost a take.

## 2026-08-19 (v0.21 — the software finally knows what the rig did)

Standing authorisation to pick the next slice, and with hardware landing in days this was the gap worth closing: **twenty versions in, every single thing this software knew was about the move it SENT.** Nothing in it had ever known what came back. That is a strange hole in a project whose whole purpose is five passes landing in the same place.

- **I went and read the firmware before designing anything**, and it changed the design three times. Stepping runs off **Timer1** (`startISR` → `Timer1.attachInterrupt`), not the main loop — which is the only reason sampling during a pass is safe at all; on a firmware that bit-banged steps the honest answer would have been "don't." Then the one that would have quietly ruined the data: **motor query 106 comes back rescaled while a motor is mid *send-to***, `curPos = isSending() ? (lastMs/ms) * curPos : curPos`, because a send silently forces quarter-stepping and restores the old microstep afterwards. A position whose *unit* changes underneath you with nothing in the reply to say so. **Query 124 answers "am I sending?"** and a key-frame move never sets the flag — so the phase that lies is the pre-pass goto, not the pass. I would have shipped a beautiful, wrong dataset.
- **The join key is the controller's percent, never host time.** Two takes started three seconds apart are still the same move at 40%. Same principle as the playhead two versions ago, and I only got it right immediately because that one had already cost me.
- **One poll, both jobs.** The renderer was already polling every 500 ms for the progress bar; the recorder folds into it, and the sample it keeps *is* the reading the UI is driven from. A second timer would have been two clocks disagreeing about when the pass was — the exact bug ADR-0025 had to fix.
- **The number this produces has a floor, and the floor is now printed next to it.** The firmware reports progress in **whole percent**, so a matched-percent comparison cannot resolve motion finer than one percent of the path — on a 4000-step axis that is 40 steps, and no averaging removes it. So a result at or under the floor is worded as a *bound*: "repeats to at least ±40 steps, and we cannot see finer." **"The rig repeats to 4 steps" and "we cannot see better than 40" are different claims and only one is true**, and the difference will matter when somebody quotes it back in three months.
- **This does not replace the tape measure**, and the report says so. Five passes with a rule measures the *endpoint* far better than this does; this measures everything in between, which the tape cannot. Both are in the bring-up report, unmerged, because merging them would have thrown away whichever one was inconvenient.
- **Nothing is invented to fill a hole**: no interpolation across a gap wider than 10% of the move, `null` outside the recorded span, and a comparison with too little overlap is refused *with the reason* — the ADR-0020 idiom again.
- **The recorder may never kill the take.** Every position read is individually guarded; a timeout is a `null` and the pass carries on. And every stop path — STOP ALL, the physical button, manual stop — **closes** the recording instead of discarding it, because a pass that was stopped is exactly the pass somebody wants to look at.
- **The sample cost is measured, not estimated.** Eight round trips per sample at 19200 baud is a real fraction of the bus and I cannot compute the rest from here, so each sample records its own wall-clock cost and the bring-up report prints the median with a sentence saying what to do if it is large. The first session gets to correct me rather than me guessing now.

### Three things the headless render caught that the assertions did not

This is the fourth time this session that *looking* beat *measuring*, and this time I had written the measurements first.

- **`overflowingRows: 0` was true and useless.** Every row in the new modal was *wrapping*, not overflowing — buttons detached from their pass and "CSV" ended up on a line of its own belonging to nobody. `scrollWidth > clientWidth` cannot see a flex wrap. Rebuilt as a grid with the buttons in their own explicit column; the first attempt at *that* put the buttons in column 1 and pushed every pass id to the right-hand edge, which the screenshot showed instantly and no assertion would have.
- **`.sheet` has `max-width: 560px`, and it is a CAP.** I wrote `style="width:660px"` and measured 560. So do three existing dialogs — they have been declaring widths they never got for several versions. Added `.sheet.wide` and a comment in the CSS, because markup asserting a width it does not receive is the same species as an export nothing calls.
- **My own stub lied to my own verification.** The `window.trace.deviationLines` browser-preview stub returned the refusal line unconditionally, so the render check reported "not comparable" over a perfectly good comparison — and for a minute I believed it. **A stub must be at least as faithful as the thing it stands in for**, which is hub invariant 23 (the simulator must be exactly as strict as the firmware) one layer up. Written into the digest as a rule rather than left as a bug I fixed.
- The overlay also drew straight off the top of its lane, because `axisScale()` was computed from the plan alone. **A deviation big enough to leave the lane is the single most important thing an overlay could show**, and it was the one thing guaranteed invisible. Same defect as v0.6's taught limits sitting outside the move's span. Recorded passes are in the scale now.

## 2026-08-19 (v0.22 — the plan type was wrong, and it decides what percent means)

Found one hour after shipping the flight recorder, by going to check an assumption in it rather than assuming.

ADR-0027 made the controller's **percent complete** load-bearing: it is the join key between two recorded passes and the anchor for the playhead. Before trusting the percent→frame mapping I went to the dispatch to confirm percent is linear in *time* rather than distance. It is. But the denominator is not what I expected:

```
if (planType() == CONT_VID) kf_run_time / (kf_getMaxMoveTime() + start_delay)
else                        kf_run_time / (kf_getMaxCamTime()  + start_delay)
kf_getMaxCamTime() = kf_getMaxMoveTime() + Camera.focusTime() + Camera.triggerTime()
```

- **`Motion_Engine.ino` defines THREE plan types** — `SMS 0`, `CONT_TL 1`, `CONT_VID 2`. Our command vocabulary described cmd 22 as *"0 = SMS, 1 = continuous"* and typed it `(mode: 0 | 1)`, which made `CONT_VID` **literally unrepresentable**. A two-valued reading of a three-valued enum, carried in a doc comment for twenty versions, and the type system then enforced the mistake.
- **The classic path sent CONT_TL. The key-frame path never set a plan type at all** — so a KF pass ran under whatever was last latched: our own classic arm, the stock app, or whatever the device booted with.
- **The damage is silent and plausible.** On any plan type but CONT_VID, percent is scaled down by the camera's focus and trigger time. Default 120 ms shutter on a 10 s move is about 1%; a 2 s exposure is 17%. The playhead would stop short while the rig finished, and every "vs plan" number would compare the rig at time *t* against the plan at a different time. Nothing would look broken. **The recorder I shipped an hour earlier would have produced a careful, well-documented, wrong dataset** — which is exactly what its own ADR says the send-to rescaling would have done, and I caught that one and not this one.
- **CONT_VID also fires the shutter once when a non-ping-pong classic program stops** — the start/stop-record semantic a video shoot expects. In CONT_TL it does not. So we were also not doing the camera thing a video rig is supposed to do.
- The official iOS app sends `NMXProgramModeVideo` for a video move. We shoot video moves. We were sending time-lapse.

**Both engines now set it explicitly, and it is READ BACK on general query 118 rather than assumed.** That distinction is the actual decision: the failure being guarded against is not a dropped write, it is *something else having set it*, and a write that succeeded tells you nothing about what a later actor did. The whole bug existed because the KF path assumed a mode it never set — assuming a mode it did set is the same mistake with one more step. The bring-up report names the latched plan type, or says **not read back** when no upload has checked it.

**The pattern worth keeping:** the bug was found by asking "what exactly is this number divided by?" about a value I had just made load-bearing. Not by a test, not by review — by following one assumption down to the source that owns it. ADR-0004 says protocol facts come from the dispatch; this is the first time that rule caught something *semantic* rather than a command number, and it is the more valuable catch of the two kinds.

## 2026-08-19 (v0.23 — ADR-0004 stops being a rule people follow)

v0.22 was found by chasing one value to the firmware and reading what it was actually divided by. That worked, and it does not scale: there are **119 command builders**, and checking them by hand is exactly the kind of work that gets done once, thoroughly, and goes stale the next time somebody re-reads the reference in a hurry.

- **The dispatch is now a committed fact table**, `packages/nmx-protocol/reference/nmx-dispatch.json`: every `case N:` each handler reaches, the firmware's own one-line comment, and a payload-width hint from which `Node.nto*` reader the case body calls. Facts and comments, not code — which is what ADR-0003 permits and what these clones have always been for. **CI never needs the GPL checkout**, because the table is data; `--extract <clone>` regenerates it.
- **`scripts/audit-vocabulary.mjs --strict` is in `check.sh`.** ADR-0004 has said since week one that protocol facts come from the dispatch. It has been a rule people follow. It is now a rule CI enforces.
- **First run, one real defect: `general.setMaxStepRate` sent general command 11**, and the firmware left a block comment where that case used to be reading *"Max step rate setting deprecated in version 0.70"* — 0.70 being the exact version this app gates on. `serMain` has no case 11, and the default branch's `response(false)` is commented out, so the device would have said **nothing at all** and stalled the one-command-in-flight queue until timeout. Nothing called it, so nothing ever hung. Removed.
- **Coverage is reported, never failed on**, and I want the reason on the record: a protocol vocabulary is a *description of a device*, and completeness there is a feature. "Nobody in this app calls it" is not evidence about a command's correctness. Today: general 31/74, motor 45/63, camera 16/26, key-frame 19/34.
- **This is deliberately NOT folded into the dead-export audit**, and the blind spot is worth naming: because `general` is itself reachable, ADR-0024's reachability analysis counts every one of its ~40 members as reachable too. It could never have found `setMaxStepRate`. Two audits, two different questions — *is it reachable* and *is it real*.

### The coverage report paid for itself twice

Reading what we do not send, two entries stood out, and they answer the question ADR-0028 raised one version earlier:

- **Key-frame query 122 returns `kf_getMaxProgramTime()` — which IS the denominator** the firmware divides percent by: `kf_getMaxMoveTime() + start_delay` on CONT_VID, `kf_getMaxCamTime() + start_delay` on anything else. We already sent it and were not using it.
- General query 125 (`totalProgramTime()`) is the classic-engine equivalent. We did not send it. Now we do.

So every recorded pass now ends by reading the device's own elapsed and its own denominator, and comparing that denominator against the duration we uploaded:

> *the device divided percent by 12000 ms while the uploaded move is 10000 ms — 20.0% longer. Percent, and every comparison joined on it, is scaled by that. Check the plan type and the start delay (ADR-0028).*

**That detects the ADR-0028 bug class from the rig rather than from a mode read-back**, which matters more than it sounds: a read-back only catches the cause we already know about. A start delay nobody set on purpose, a firmware variant, or something none of us has thought of shows up here as the same disagreement, in the same sentence. The check is quiet when there is nothing to say, because a line that prints every pass is a line people stop reading.

## 2026-08-19 (v0.24 — a taught limit is a step count, and the origin can move)

Straight out of v0.23's coverage report — the list of commands the firmware answers that we never asked. Three of them read together describe a hole underneath ADR-0013, which is the one guard between a camera package and a mechanical end stop.

- **A taught limit is a step count, and a step count is not a place.** It is a place *relative to the controller's origin*. `ee_load_curPos` **defaults to false** in the firmware — boot only restores `currentPos` when that flag is set — so an NMX unplugged between sessions comes back with its origin reset while our preferences still hold `{min: -12000, max: 40000}`. The app would enforce those numbers against a rail they no longer describe. **The guard rail silently moves, and the operator still believes in it, which is worse than having no limits at all** — no limits at least leaves you careful.
- **General query 119 is a one-shot latch**, and this shaped the whole design:

  ```c
  byte powerCycled() {
    static byte cycled = true;
    byte response = cycled;
    cycled = false;
    return(response);
  }
  ```

  Whoever asks first consumes it. So the app asks **exactly once per connection** — asking twice throws away the only chance to hear it — and **a `false` is never phrased as an all-clear**, because the stock app or an earlier session connecting first would have taken it. That is what the `fragile` verdict is for: *"no power cycle was reported to us — but that flag is consumed by whoever reads it first."*
- **Voided limits are not deleted, they stop being acted on.** The numbers stay on screen; the operator may know perfectly well they are still right. What changes is that the axis reads as **untaught**, which re-arms the ADR-0023 creep cap — and that is not a coincidence: you have just learned you know less than you thought about where the carriage is, which is exactly the moment that cap exists for. A slow collision is a noise.
- **Teaching a bound clears it**, because teaching a bound *is* the operator stating where the rig is against this origin. Same self-clearing shape as the creep cap, same reason: an override you have to remember to turn off is one you leave on. Or they can say so explicitly, once, on a button — on their say-so, not the controller's.
- **The app never writes the controller's EEPROM on its own.** Command 30 fixes the fragile case permanently, and it is offered on a button with the sentence next to it. It is somebody else's device, shared with whatever else they run it with, and a tool that quietly changes persistent settings is a tool you stop trusting.
- **The simulator implements the latch exactly** — true once, false forever after, `restoresPosition` false by default like the firmware. A simulator that answered true every time would have made the app look correct and hidden the entire problem. Hub invariant 23, again.

### The rail height budget bit back, and measuring caught it

The first version of the notice was the full five-line paragraph, in the rail, where the safety statement belongs. It measured 212px and pushed the **pass log off the bottom of the window** — `railScroll: true`. The HUB has said since v0.10 that the rail budget is spent and new material goes in a modal; I wrote it into the rail anyway because it felt too important to hide, which is exactly the reasoning that fills a rail. Now: **one line** in the rail (`⚠ Limits not enforced` + `Why…`), 34px, and the sentence, the controller's own answers and both actions in a dialog. Second attempt at the line wrapped across three rows mid-phrase; the screenshot showed it, the assertion did not.

**Also fixed while in there:** the nmx-protocol digest still listed `withinLimit` / `clampToLimit` in the limits section. The v0.17 dead-export audit deleted both and nobody updated the line. That is the third instance of inventory-and-negative prose rotting while the additive prose stays current — and this time I found it by reading the section I was about to edit, which is the only reliable trigger I have.

## Open items
- **NEXT (software):** sustained-cue tail dragging, cue duplication, a saved cue-preset library. MIDI last, and only on real need (every Node binding is native; Web MIDI would breach ADR-0007).
- **NEXT (hardware, imminent): real NMX contact** — usbserial port, Connect, report firmware version (gate expects v70). Then KF vs classic replay fidelity (ratifies ADR-0006), **tune the soft-limit margins**, and **measure the rig calibration** (ADR-0015 export is untrustworthy until it exists).
- Hardware: stock-app sanity check; payload/speed ceilings for the Blackmagic rig. Build a GRAFFIK-TRIG board and verify Tier 2 against a scope. Verify the DMX path against a real Enttec Pro and a lamp — the simulated widget proves the framing, not the wiring. **Then Phase 7 — the first lens motor**: one axis only, TMC2209 + 0.8 MOD pinion, StallGuard tuned (or a switch fitted, and no shame in it), calibration stable across three runs, top speed measured rather than assumed, and a five-pass repeatability check photographed against tape on the barrel.
- Hardware-free backlog: tagged release with .dmg, Windows CI job, Apple Developer ID signing/notarization.
- If MAC_SIENA_AI chat's knowledge-system conventions get shared into this project, reconcile skill + docs to them.
